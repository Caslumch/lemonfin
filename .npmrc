shamefully-hoist=true
# Layout plano (npm-like) — recomendado pela Expo para pnpm. Sem isso, o eas-cli
# invoca o `expo config` direto (fora do shim do pnpm) e config-plugins de libs
# como @react-native-community/datetimepicker não resolvem @expo/config-plugins.
node-linker=hoisted
